namespace LP2_P1
{
    /// <summary>
    /// Enumerator that holds every possible value to the order's state
    /// </summary>
    public enum OrderState
    {
        Ascending,
        Descending,
        Unordered
    };
}