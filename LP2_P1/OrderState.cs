namespace LP2_P1
{
    /// <summary>
    /// The possible states the TitleSearch list order
    /// </summary>
    public enum OrderState
    {
        Ascending,
        Descending,
        Unordered
    };
}